{
  "name": "contrail-cloudflare-workers-example",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "generate": "tsx generate.ts",
    "generate:pull": "tsx generate.ts && lex-cli pull && tsx generate.ts && lex-cli pull && lex-cli generate",
    "sync": "tsx sync.ts",
    "sync:remote": "tsx sync.ts --remote"
  },
  "dependencies": {
    "@atmo-dev/contrail": "^0.0.3"
  },
  "devDependencies": {
    "@atcute/lex-cli": "^2.5.3",
    "@atcute/lexicon-doc": "^2.1.2",
    "@cloudflare/workers-types": "^4.20250124.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.63.0"
  }
}
