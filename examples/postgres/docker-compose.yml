services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: contrail
      POSTGRES_USER: contrail
      POSTGRES_PASSWORD: contrail
    ports:
      - "${PG_PORT:-5433}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
