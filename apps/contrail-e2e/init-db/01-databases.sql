-- Create both databases used by this example:
--   plc      — DID registry state for the devnet PLC server
--   contrail — Contrail's own indexed records
CREATE DATABASE plc;
CREATE DATABASE contrail;
